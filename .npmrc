# Prevent @types/react and @types/react-dom from being hoisted to avoid
# React 18 / React 19 type conflicts between Expo (mobile) and Next.js (web).
# Each workspace package resolves these via its own devDependencies instead.
hoist-pattern[]=!@types/react
hoist-pattern[]=!@types/react-dom
